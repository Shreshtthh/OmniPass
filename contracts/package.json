{
  "name": "omnipass-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for OmniPass cross-chain credential system",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy-and-update.ts --network zetachain_testnet",
    "deploy:local": "hardhat run scripts/deploy-and-update.ts --network localhost",
    "redeploy": "npm run compile && npm run deploy && echo '🎉 Contract redeployed and all configs updated!'",
    "setup": "npm run compile && npm run deploy && npm run post-deploy",
    "post-deploy": "echo '✅ Don\\'t forget to restart your backend and frontend servers!'",
    "verify": "hardhat verify --network zetachain_testnet",
    "verify:local": "echo 'No verification needed for localhost'",
    "size": "hardhat size-contracts",
    "gas": "REPORT_GAS=true hardhat test",
    "lint": "solhint 'src/**/*.sol'",
    "lint:fix": "solhint 'src/**/*.sol' --fix",
    "prettier": "prettier --write 'src/**/*.sol' 'test/**/*.ts' 'scripts/**/*.ts'",
    "prettier:check": "prettier --check 'src/**/*.sol' 'test/**/*.ts' 'scripts/**/*.ts'",
    "flatten": "hardhat flatten src/OmniPassCredential.sol > flattened/OmniPassCredential_flat.sol",
    "check-deployment": "node scripts/check-deployment.js",
    "authorize-analyzer": "hardhat run scripts/authorize-analyzer.ts --network zetachain_testnet",
    "test-integration": "hardhat run scripts/test-contract-integration.ts --network zetachain_testnet",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:all-testnets": "echo 'Note: Only deploy to ZetaChain for Universal Contract'",
    "get-testnet-tokens": "echo '🔸 ZetaChain: https://labs.zetachain.com/get-zeta\\n🔸 Sepolia: https://sepoliafaucet.com/\\n🔸 Amoy: https://faucet.polygon.technology/'"
  },
  "keywords": [
    "defi",
    "cross-chain",
    "credentials",
    "zetachain",
    "reputation",
    "access-control",
    "testnet",
    "mvp",
    "universal-contract"
  ],
  "author": "OmniPass Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.8",
    "@openzeppelin/hardhat-upgrades": "^3.2.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.5",
    "hardhat-contract-sizer": "^2.10.0",
    "hardhat-gas-reporter": "^1.0.10",
    "prettier": "^3.3.2",
    "prettier-plugin-solidity": "^1.3.1",
    "solhint": "^5.0.1",
    "solidity-coverage": "^0.8.12",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "@openzeppelin/contracts-upgradeable": "^4.9.3",
    "@zetachain/protocol-contracts": "^6.0.0",
    "ethers": "^6.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "your-repo-url"
  }
}
